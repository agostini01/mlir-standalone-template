# RUN: standalone-compiler %s -emit=ast 2>&1 | FileCheck %s

def main() {
# CHECK-LABEL: Proto 'main'
  var a = [[1, 2, 3], [4, 5, 6]];
  var b<2, 3> = [1, 2, 3, 4, 5, 6];

# CHECK: Call 'transpose'
# CHECK: Call 'transpose'
  print(transpose(a) * transpose(b));
}
